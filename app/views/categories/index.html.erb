<h1 class="text-3xl font-bold my-4 text-center"><%= t('.title') %></h1>

<%= render partial: 'category', collection: @categories %>

<div class="flex justify-center my-4">
  <%= link_to t('.new'), new_category_path, class: "bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded" %>
</div>
