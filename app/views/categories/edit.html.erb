<h1 class="text-2xl font-bold my-4"><%= t('.title') %></h1>

<%= render "form", category: @category %>

<div class="my-4">
  <%= link_to t('common.back'), categories_path, class: "font-bold text-teal-500" %>
</div>
